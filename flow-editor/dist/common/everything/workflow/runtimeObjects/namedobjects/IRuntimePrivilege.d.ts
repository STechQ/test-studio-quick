export interface IRuntimePrivilege {
    id: string; /** Full privilege refId in the form `<<privilege:modelId>>:objectId`. Stored verbatim so downstream checks can compare against refIds without re-resolution. */
}
//# sourceMappingURL=IRuntimePrivilege.d.ts.map